export interface ResponseModel{
    success:boolean;
    message:string;
}
//buradaki success ve message listResponse modeller de ortak, farklı bir üstkatman response model gelirse ilerde kullanilabilir